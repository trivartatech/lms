// Local entry file so Metro (running from packages/mobile as projectRoot) can
// resolve the expo-router entry even when expo-router is hoisted to the
// monorepo root and not present under packages/mobile/node_modules.
import 'expo-router/entry'
